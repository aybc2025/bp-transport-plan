// Leaflet handles popups natively via layer.bindPopup() in TransitMap.
// This file is kept for architecture consistency — can be extended
// for custom React-rendered popups if needed.
export default function RoutePopup() {
  return null;
}
